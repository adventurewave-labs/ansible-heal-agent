# Runtime
PyYAML>=6.0
click>=8.1
rich>=13.7
# Round-trip YAML so the agent's structural edits preserve comments,
# key order and indentation instead of rewriting whole files.
ruamel.yaml>=0.18

# The LLM bridge speaks HTTP with urllib from the standard library, so no
# provider SDK is required. Configure a backend with ANTHROPIC_API_KEY,
# OPENROUTER_API_KEY, or a z-ai CLI on PATH.

# Dev (optional)
pytest>=8.0
pytest-cov>=5.0
# `make lint` runs ruff; CI pins the same tool.
ruff>=0.6

# Integration tests drive a real ansible-playbook process. Without this the
# tests in tests/test_real_ansible.py skip rather than fail, but then nothing
# verifies that PIPELINE_RUNNER=real actually works.
ansible-core>=2.16
